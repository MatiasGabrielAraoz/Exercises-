#include "include/io.h"
#include "include/types.h"


void kmain(){
	// for (size_t i = 0; i < 80; i++){
	// 	for (size_t j = 0; j < 25; j++){
	// 		write_char_to_frame_buffer('A', j, i, RED, WHITE);
	// 	}
	// }
	
	move_cursor(200);

	return;
}
